# SuiteMate V3 — live testing log

One line per live session against the locked record (account `6998262`,
Sales Order `id=16342809`, and the same record with `&e=T`). A session with no
save still leaves a line. Save lines additionally record the four-eyes gate
verdict.

| Timestamp | Milestone | Exercised | Evidence | Gate verdict |
|---|---|---|---|---|
| 2026-08-02 23:07 AEST | M1 | Dossier §12 probes 1, 2, 3, 4, 6, 6b, 6c, 6d, 7, 9, 10, 11, 12 and probe 8 (Gate A) on `id=16342809&e=T`; read-only except three authorized in-page interactions — probe 8's Gate A cell permutation plus its quantity edit and OK commit on line 3, probe 10's Insert/Remove/Cancel cycle, and probe 11's line-open and OK commit with the Quantity column hidden (all discarded by navigate-away teardown, owner-confirmed); probe 5 deferred (POST-body half requires a save; read-only half not run — open gap for M1.5) | Raw transcripts in `.superpowers/sdd/2026-08-02-edit-mode-table-enhancements/probe-transcripts.md`; interpreted verdict in the M1 checkpoint entry (`save/CHECKPOINTS.md`) | No save; four-eyes gate not invoked. Gate A: REFRAME (model-driven regeneration — repaint replaces, never patches; corruption not manifest; M4 blocked pending M1.5 identity + Gate A′) — see checkpoint. Native drag-order (probe 6b): no |
| 2026-08-03 08:55 AEST | M1.5 | T6 live re-probe on `id=16342809&e=T`: read-only mount proof (no line opened, no permutation, no save); toggle cycled + fresh reload; diagnosis probe for the mount failure | `probe-transcripts.md` "M1.5 live re-probe" | No save; four-eyes gate not invoked. MOUNT FAIL — root cause confirmed: machine mini-form boundary (item_form vs main_form) blocks the core's closest("form") input lookup; T6a proposed. Zero console errors; record byte-identical |
| 2026-08-03 09:22 AEST | M1.5 | T6 re-run on `id=16342809&e=T` + Tier-4 View Mode sweep on the view URL (extension at dd8143a): MOUNT PASS (bound=1, owned=1, stable, 0 console); sort/filter/hide-show/personalize exercised computed-level and reversed; widths persistence observed incl. reload; owner data delta zero (reload-verified). Export/badges/drag-reorder/resize remain owed (verdict §B) | `probe-transcripts.md` "M1.5 live re-probe RE-RUN" + independent verdict in the M1.5 checkpoint | No save; four-eyes gate not invoked. T6 gate: MOUNT PASS (verdict of record). Axis proven node-side on byte-identical inputs; idle-writes proven by code |
| 2026-08-04 01:05 AEST | M3 | Hide/show on `id=16342809&e=T` (extension at bc8e370): safety triple verified view-side then edit-side; control bar computed inline-flex 422×24; stored 4-column hidden set re-applied on mount (persistence across reload); Quantity hidden via menu (the plan calls it required; the later required-marker probe shows this form stars only Item and Tax Code) — header + body computed none, chip added; menu outside-click close and inside-click retention; open line 1 → force-reveal ALL hidden columns computed table-cell; OK commit with columns hidden → values preserved ("1" readable in hidden cells), hidden set re-applied, pristine entry row correctly not revealing; Remove line → 9→8 renumber, hidden re-applied; navigate-away discard proven (view shows 9 data rows); re-enter → menu seeds from stored set, Quantity re-shown, exact pre-session personalization restored. Fixture (real sheets, no extension): stamped bar inline-flex 185×24 vs unstamped bar none 0×0; col-hidden class → computed none header+body, clean restore. Deviations disclosed: toast unobserved (timing); add-line-by-typing skipped (unit-pinned quadrants; sourcing risk); \_fs/main_form plan snippet superseded by M1.5; view-key byte-baseline not captured pre-session (route complement + source purity stand in) | This log line + `save/CHECKPOINTS.md` M3 entry; independent adjudication by Opus 5 verifier recorded there | No save; four-eyes gate not invoked. Owner's offer to save (to suppress the beforeunload prompt) DECLINED — no unsaved changes existed; the prompt is NetSuite's blanket edit-page guard, not pending state. Adjudication: MECHANISM PASS on every exercised gate; CHECKPOINT DECLARATION PENDING R1–R5 (edit-a-field + recalc; add-a-line; toast capture; behavioural Tier-4 incl. Export; storage byte-baseline or M5 binding) |
| 2026-08-04 09:20 AEST | M3 close-out | Combined session on `id=16342809&e=T` at `c09997a` (both owner directives live): safety triple verified; Item + Tax Code checked+disabled+titled in the Columns menu, all 41 others enabled; Escape closes the menu. HIDDEN STAYS HIDDEN through every interaction: open line (3 hidden columns held), field typing, qty 1→2 commit (amount recalculated 32.00/2.91 + reorder advisory = server recalc; hidden re-applied through the commit repaint, body cells computed none), revert commit 2→1, entry-row typing "MCH14" (the owner's reported scenario — held), two visible validation alerts with columns hidden, item sourcing rewrite (held), add committed row 9→10 with the hide re-applied to the NEW row, remove 10→9 (held). R3 converted by design: the reveal toast no longer exists. Navigate-away discard proven (view: 9 data rows, record byte-identical). R4 View behavioural: sort Z-A real reorder + Clear sort restores native exactly; filter hides exactly the unchecked value at computed level + Clear filter restores; Personalize/Done cycle; width drag 77→107→77px; **Export view: "Exported 9 visible rows"** (data rows, not headers-only); tab title formatted; zero SuiteMate console errors both modes (4 packship-bundle exceptions, no SuiteMate frames) | `save/CHECKPOINTS.md` M3 declaration | No save; four-eyes gate not invoked. R5: byte-baseline not captured (owner skipped the console step; unreachable from the bridge) — per the adjudicator's alternative, the byte-identical claim is struck from M3 and bound as a hard M5 gate |
